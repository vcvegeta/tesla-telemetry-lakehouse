FROM apache/airflow:2.9.3

USER root

# Install Docker CLI for docker exec commands
RUN apt-get update && \
    apt-get install -y docker.io && \
    rm -rf /var/lib/apt/lists/*

USER airflow

# Copy DAGs
COPY airflow/dags /opt/airflow/dags

WORKDIR /opt/airflow
